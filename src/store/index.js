import { createStore } from "vuex";

const store = createStore({
  state: {
    // 在这里定义你的状态
  },
  mutations: {
    // 在这里定义你的状态变更函数
  },
  actions: {
    // 在这里定义你的异步操作
  },
  getters: {
    // 在这里定义你的状态获取函数
  }
});

export default store;
